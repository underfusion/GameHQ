// The shipped default. Skin.qml already carries these values as its defaults,
// so this overrides nothing — it exists to name the baseline and keep the skin
// list uniform.
Skin {
    label: "Dark"
}
