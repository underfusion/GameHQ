# Consumed by Playnite's add-on installer and add-on database (see plan
# items p6-1/p6-2). Kept in sync with extension.yaml + VERSION by
# packaging/verify.ps1. PackageUrl is populated once the first playnite-v*
# GitHub release exists (p6-1) — left blank until then.
AddonId: GameHQ_Integration
Packages:
  - Version: 0.4.12
    RequiredApiVersion: 6.16.0
    ReleaseDate: 2026-07-22
    PackageUrl: "https://github.com/underfusion/GameHQ/releases/download/v0.7.0/GameHQ_Playnite_Integration_0_4_12.pext"
    Changelog:
      - Connects Playnite to GameHQ over the local v1 protocol.
      - Forwards game lifecycle state and launches GameHQ when needed.
      - Adds connection, version, path and privacy diagnostics.
      - Adds the pinned test-key-only Ed25519 verifier and shared trust vectors.
