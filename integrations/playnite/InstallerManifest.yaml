# Consumed by Playnite's add-on installer and add-on database (see plan
# items p6-1/p6-2). Kept in sync with extension.yaml + VERSION by
# packaging/verify.ps1. PackageUrl is populated once the first playnite-v*
# GitHub release exists (p6-1) — left blank until then.
AddonId: GameHQ_Integration
Packages:
  - Version: 0.4.12
    RequiredApiVersion: 6.16.0
    ReleaseDate: 2026-07-22
    PackageUrl: ""
    Changelog: |
      Connects Playnite to GameHQ over the local v1 protocol, forwards game
      lifecycle state, launches GameHQ when needed, and provides connection,
      version, path and privacy diagnostics. Version 0.4.12 adds the pinned,
      test-key-only Ed25519 release-manifest verifier and shared trust vectors.
